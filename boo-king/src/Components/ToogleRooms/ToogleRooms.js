import React from 'react'

export const ToogleRooms = ({items , open}) => {

    console.log("items" , items , "id" , items.map((item)=>(

        item._id
    )))
  return (
    <div>ToogleRooms</div>
  )
}
